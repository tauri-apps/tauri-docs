---
import { getEntryBySlug } from 'astro:content'
import Layout from '../../../../layouts/Layout.astro'
const entry = await getEntryBySlug('api-core-cli-1', 'index')
const { Content } = await entry.render()
---

<Layout title={entry.slug}>
  <Content />
</Layout>
