---
import { addHeadingLinks, renderPluginPermissionsMarkdown } from '../utils/pluginPermissions';

interface Props {
  plugin: string;
}

const { plugin } = Astro.props;

const code = addHeadingLinks(await renderPluginPermissionsMarkdown(plugin));
---

<Fragment set:html={code} />
