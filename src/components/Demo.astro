---
import { Card } from '@astrojs/starlight/components';
const enabled = process.env.TAURI_DEBUG && Astro.slots.has('default');
---

<Card title="Demo" icon="document">
  {enabled ? <slot /> : <p>Try it in the Tauri Docs app (coming soon)</p>}
</Card>
